import React from "react";
import Button from "../Button";

const NavBar = () => {
  return (
    <div>
      <h1>Logo</h1>
      <div>
        <a href="#">Home</a>
        <a href="#">About</a>
        <a href="#">Contact</a>
      </div>
      <Button />
    </div>
  );
};

export default NavBar;
